{
  "name": "applyflow-backend-workers",
  "version": "1.0.0",
  "description": "ApplyFlow ATS — Cloudflare Workers Serverless Backend",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^2.4.3",
    "hono": "^4.6.14",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.7.2",
    "vitest": "^5.0.0",
    "wrangler": "^3.99.0"
  }
}
